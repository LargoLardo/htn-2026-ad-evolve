{
  "name": "advolve-cloudflare",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "test": "vitest run",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply advolve-index --local",
    "migrate:remote": "wrangler d1 migrations apply advolve-index --remote"
  },
  "dependencies": { "jose": "^6.0.0", "fast-png": "^7.0.0" },
  "devDependencies": { "wrangler": "^4.0.0", "@cloudflare/vitest-pool-workers": "^0.22.0", "vitest": "^4.1.0" }
}
